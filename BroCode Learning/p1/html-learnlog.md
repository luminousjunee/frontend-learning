# HTML Learning Log of p1
In p1 (progress 1), I have learned the following:
<br><br>
- What HTML stands for
<br><br>
"HyperText Markup Language"
<br><br>
- What HTML does
<br><br>
Adds structure to a webpage (Example by BroCode: If a house were being built, HTML is the foundation, the skeletal structure to support all content on a website.)
<br><br>
- What is needed to begin working with HTML (and CSS)
<br><br>
Need a modern web browser, like Google Chrome, Microsoft Edge, Firefox, or Safari, as well as a text editor (VS Code)
<br><br>
# HTML Tags/Elements
- Tags (＜tag＞＜/tag＞), also known as "markup", is used to convery a web browser, how some content should be displayed.
Example: ＜p＞Hello World!＜/p＞ (Will display a paragraph text reading "Hello World!")
<br><br>
- Text tags (headings (＜h1＞, ＜h2＞, ＜h3＞, ＜h4＞, ＜h5＞, ＜h6＞, ＜h7＞), paragraph (＜p＞), hyperlink (＜a＞))
<br><br>
# Beginning HTML Coding
- ＜!DOCTYPE html＞ (A declaration that the file is an HTML 5 document, which is the most recent version.)
<br><br>
- ＜html＞(open tag) ＜/html＞ (ending tag) (＜html＞＜/html＞ is a pair of HTML tags that represents the root element of an HTML page.)
<br><br>
- ＜head＞＜/head＞ (The head element should be placed inside the html tags (＜html＞＜/html＞). The head element contains information about a webpage. For example, a title (＜title＞＜/title＞), which is the website title.)
<br><br>
＜title＞My first website＜/title＞ (This will replace the website's title with "My first website".)
<br><br>
- ＜body＞＜/body＞ (The body tag contains visible content in a webpage. For example, ＜h1＞Welcome to my website＜/h1＞ (this will add a large heading inside the website showing a text that says "Welcome to my website".))
<br><br>
- ＜p＞＜/p＞ (explained above, but this tag is for paragraphs.)
<br><br>
- ＜pre＞＜/pre＞ (pre is short for "pre-formatted", which will retain line breaks or spaces within the pre-formatted tag.)
<br><br>
- ＜hr＞ (adds a horizonal line across the webpage, depending on where the tag is.)
<br><br>
- ＜!-- --＞ (a comment, does not appear on the web page, comments are used for yourself or for other developers) (example: ＜!-- THIS IS A COMMENT --＞)
<br><br>
# EXERCISE
By BroCode, the first exercise is to write my favorite lyrics of a song. I have made this through the index.html inside p1.